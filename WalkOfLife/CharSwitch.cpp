#include "CharSwitch.h"

void CharSwitch::switchCharState()
{
	if (charstate == 0)
	{
		
	}
	else if (charstate == 1)
	{
		//delete current charater model.
		//move new character to current position.
		//theCharacter->setVertexbuffer(vBuffer2);

	}
	else if (charstate == 2)
	{
		//theCharacter->setVertexbuffer(vBuffer3);
	}
}